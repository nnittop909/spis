<div class="modal-header">
  <h5 class="modal-title">Update Minute</h5>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
<% remote ||= false %>
<%= simple_form_for @minute, html: { autocomplete: "off"}, remote: remote, url: minute_path(id: @minute.id), method: :patch do |f| %>
  <%= render 'form', f: f %>
<% end %>