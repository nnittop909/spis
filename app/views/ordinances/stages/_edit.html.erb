<div class="modal-header">
  <h5 class="modal-title">Update Ordinance Stage</h5>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
<% remote ||= false %>
<%= simple_form_for @stage, html: { autocomplete: "off"}, remote: remote, url: ordinance_stage_path(ordinance_id: @ordinance.id, id: @stage.id), method: :patch do |f| %>
  <%= render 'form', f: f %>
<% end %>