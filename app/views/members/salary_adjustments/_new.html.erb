<div class="modal-header">
  <h5 class="modal-title">New Salary Adjustment</h5>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
<% remote ||= false %>
<%= simple_form_for @salary_adjustment, html: { autocomplete: "off" }, remote: remote, url: member_salary_adjustments_path(member_id: @member.id), method: :post do |f| %>
  <%= render 'form', f: f %>
<% end %>