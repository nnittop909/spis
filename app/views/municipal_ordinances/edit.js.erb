$("#remote-modal").find(".modal-content").html("<%= escape_javascript(render 'edit', remote: true) %>");
$("#remote-modal").modal();

$(document).ready(function() {
  $('.dateSelect').flatpickr({
    altInput: true,
    altFormat: "F j, Y",
    dateFormat: "Y-m-d"
  });
});