<div class="modal-header">
  <h5 class="modal-title">Update Committee</h5>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
<% remote ||= false %>
<%= simple_form_for @committee, validate: true, html: { autocomplete: "off" }, remote: remote, url: committee_path(@committee), method: :patch do |f| %>
  <%= render 'form', f: f %>
<% end %>