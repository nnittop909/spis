<div class="modal-header">
  <h5 class="modal-title">Update Party</h5>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
<% remote ||= false %>
<%= simple_form_for @political_party, html: { autocomplete: "off"}, remote: remote, url: settings_political_party_path(id: @political_party.id), method: :patch do |f| %>
  <%= render 'form', f: f %>
<% end %>